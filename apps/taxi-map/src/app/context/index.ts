import { createContext } from "react";
// import { environment } from "../../environments/environment";
// import { OfficeLocation } from "../models";

export const TaxisLocationContext = createContext<any>(null);

export const OfficeLocationContext = createContext<any>(null);
